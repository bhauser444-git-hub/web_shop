# web_shop
Web shop with Node.js

Precondition:
(execute in normal cmd Terminal in the web server directory)
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process
npm install mysql2

if using web_server2.js, add ejs (Embedded JavaScript):
npm install ejs

in addition, to use the dotenv package:
npm install dotenv


